% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_random_graphs.R
\name{fit_random_graphs}
\alias{fit_random_graphs}
\title{Batch Fit DSEM on Randomized Graphs}
\usage{
fit_random_graphs(batch_results, data.file, cores = 1, latent_dict = NULL)
}
\arguments{
\item{batch_results}{List. The output object from \code{evaluate_random_graphs} 
containing \code{$graphs}, \code{$lags}, and \code{$diagnostics}.}

\item{data.file}{Character. Path to the simulated time series data. Supports `.csv` or `.rds`.}

\item{cores}{Integer. Number of CPU cores to use for parallel processing. Defaults to 1 (sequential).}

\item{latent_dict}{List. Optional named list mapping latent variables to their indicators.}
}
\value{
A named list of fitted DSEM objects, corresponding to each graph in the batch.
        If a specific topology fails to converge, its list element will be NULL.
}
\description{
Takes the output from \code{evaluate_random_graphs} and fits a generalized 
DSEM model to each generated graph structure using the provided simulated 
data and temporal lags.
}
\examples{
\dontrun{
# 1. Generate unique random graphs
batch_results <- evaluate_random_graphs(true_adj, true_lags, N = 100, deduplicate = TRUE)

# 2. Fit them all to the simulated data in parallel (e.g., using 4 cores)
batch_fits <- fit_random_graphs(batch_results, "sim_data.rds", cores = 4)

# Inspect the first model's estimates
print(batch_fits[[1]]$estimates)
}
}
